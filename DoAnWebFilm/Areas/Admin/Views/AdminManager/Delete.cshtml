@model DoAnWebFilm.Models.AdminPro

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@*<h2>Delete</h2>

    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>AdminPro</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.ten_admin)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ten_admin)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.mat_khau_admin)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.mat_khau_admin)
            </dd>

        </dl>

        @using (Html.BeginForm()) {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>*@
<section>
    <div class="container-md">
        <div class="my-5 ">

            <div class="bg-danger text-white py-2 text-center mb-3 rounded">
                <h4 class=" mb-0">Bạn có thực sự muốn xóa?</h4>
            </div>
            <div class="card">
                <div class="card-header bg-info d-flex justify-content-between">
                    <div class="h4 mb-0 mt-1">
                        Xóa tài khoản admin
                    </div>

                </div>
                <div class="card-body">
                    <dl class="dl-horizontal">
                        <dt>
                          Tài khoản admin
                        </dt>
                        <dd>
                            @Html.DisplayFor(model => model.ten_admin)
                        </dd>

                        <dt>
                            Mật khẩu admin
                        </dt>
                        <dd>
                            @Html.DisplayFor(model => model.mat_khau_admin)
                        </dd>
                    </dl>

                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()
                        <div>
                            <input type="submit" value="Xóa" class="btn btn-primary" />
                            @Html.ActionLink("Thoát", "Index", null, new { @class = "btn btn-danger" })
                        </div>
                    }

                </div>
            </div>

        </div>

    </div>
</section>
