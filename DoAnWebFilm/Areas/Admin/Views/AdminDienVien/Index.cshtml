@model IEnumerable<DoAnWebFilm.Models.DienVien>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@*<h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th >
                @Html.DisplayNameFor(model => model.ten_dien_vien)
            </th>
            <th style="width:500px">
                @Html.DisplayNameFor(model => model.anh_bia)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ten_dien_vien)
            </td>
            <td>
                <img class="img-thumbnail" src="@Url.Content("~/HinhDienVien/"+  item.anh_bia)" alt="Loading..." />
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id_dien_vien }) |
                @Html.ActionLink("Details", "Details", new { id=item.id_dien_vien }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id_dien_vien })
            </td>
        </tr>
    }

    </table>*@

<section>
    <div class="container-md">
        <div class="my-5 ">
            <div class="my-3 d-flex justify-content-end">

                <div class="">
                    @*<form class="d-flex">
                        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                            <button class="btn btn-info" type="submit">Search</button>
                        </form>*@
                    </div>

                </div>
            <div class="card">
                <div class="card-header bg-info d-flex justify-content-between">
                    <div class="h4 mb-0 mt-1">
                        Quản Lý Diễn Viên
                    </div>
                    <div>
                        @Html.ActionLink("Thêm Diễn Viên", "Create", null, new { @class = "btn btn-success mx-1" })

                    </div>
                </div>
                <div class="card-body">
                    <table class="table table-striped table-hover">
                        <thead>
                            <tr class="text-center">
                                <th scope="col">#</th>
                                <th scope="col">Tên diễn viên</th>
                                <th scope="col">Hình diễn viên</th>
                                <th scope="col">Thao tác</th>

                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                            <tr class="text-center">

                                <th scope="col">   @Html.DisplayFor(modelItem => item.id_dien_vien)</th>
                                <td>
                                    @Html.DisplayFor(modelItem => item.ten_dien_vien)
                                </td>
                                <td>
                                    <img class="img-thumbnail" src="@Url.Content("~/HinhDienVien/"+  item.anh_bia)" width="100" alt="Loading..." />
                                </td>

                                <td>

                                    <a href="@Url.Action("Edit", "AdminDienVien", new { id = item.id_dien_vien })" class="btn btn-warning mx-1">
                                        <i class="fas fa-pen text-white"></i>
                                    </a>
                                    @*<a href="@Url.Action("Details", "AdminDienVien", new { id = item.id_dien_vien })" class="btn btn-info mx-1">
                                        <i class="fas fa-file-alt text-white "></i>
                                    </a>*@
                                    <a href="@Url.Action("Delete", "AdminDienVien", new { id = item.id_dien_vien })" class="btn btn-danger mx-1">
                                        <i class="fas fa-trash-alt text-white"></i>
                                    </a>

                                </td>
                            </tr>

                            }
                        </tbody>
                    </table>

                </div>
            </div>


        </div>

    </div>
</section>


